<UserControl x:Class="TMS.Controls.Admin.AddScheduleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="800"
             FontFamily="Segoe UI">

    <!-- Gray Background -->
    <Grid Background="#F0F2F5">

        <!-- Centered White Card -->
        <Border Background="White" 
                Width="500" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center" 
                CornerRadius="12"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                Margin="0,20">

            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.1" Color="Black"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Header -->
                    <RowDefinition Height="*"/>
                    <!-- Scrollable Content -->
                    <RowDefinition Height="Auto"/>
                    <!-- Footer Button -->
                </Grid.RowDefinitions>

                <!-- 1. HEADER -->
                <Border Grid.Row="0" 
                        Background="#1E3A8A" 
                        CornerRadius="12,12,0,0" 
                        Padding="20">
                    <TextBlock Text="Add New Schedule" 
                               Foreground="White" 
                               FontSize="20" 
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- 2. SCROLLABLE FORM -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30,20,30,10">
                    <StackPanel>

                        <!-- TYPE SELECTION -->
                        <TextBlock Text="Schedule Type" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,10"/>
                        <Border Background="#F7F9FC" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15,10" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="rbOneTime" Content="One-Time" IsChecked="True" Checked="ScheduleType_Checked" Margin="0,0,30,0" FontWeight="SemiBold"/>
                                <RadioButton x:Name="rbRecurring" Content="Recurring" Checked="ScheduleType_Checked" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>

                        <!-- COMMON FIELDS -->
                        <TextBlock Text="Select Bus" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                        <Border Background="#F7F9FC" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,15">
                            <ComboBox x:Name="cmbBusAdd" Padding="10" Background="Transparent" BorderThickness="0"/>
                        </Border>

                        <TextBlock Text="Select Route" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                        <Border Background="#F7F9FC" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,15">
                            <ComboBox x:Name="cmbRouteAdd" Padding="10" Background="Transparent" BorderThickness="0" SelectionChanged="cmbRouteAdd_SelectionChanged"/>
                        </Border>

                        <TextBlock Text="Price" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                        <Border Background="#F7F9FC" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,20">
                            <TextBox x:Name="txtPriceAdd" Padding="10" Background="Transparent" BorderThickness="0" ToolTip="Enter price"/>
                        </Border>

                        <!-- ========================= -->
                        <!-- ONE-TIME PANEL -->
                        <!-- ========================= -->
                        <StackPanel x:Name="OneTimePanel">
                            <TextBlock Text="One-Time Details" FontSize="16" FontWeight="Bold" Foreground="#1E3A8A" Margin="0,0,0,15"/>

                            <!-- Departure -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Departure Date" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <DatePicker x:Name="dpDepartureDateAdd" Padding="5" SelectedDateChanged="DepartureDateChanged"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Departure Time" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <xctk:TimePicker x:Name="tpDepartureTimeAdd" Padding="5" ValueChanged="DepartureTimeChanged"/>
                                </StackPanel>
                            </Grid>

                            <!-- Arrival -->
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Arrival Date (Auto)" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <DatePicker x:Name="dpArrivalDateAdd" Padding="5" IsEnabled="False" Background="#EEEEEE"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Arrival Time (Auto)" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <xctk:TimePicker x:Name="tpArrivalTimeAdd" Padding="5" IsEnabled="False" Background="#EEEEEE"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>

                        <!-- ========================= -->
                        <!-- RECURRING PANEL -->
                        <!-- ========================= -->
                        <StackPanel x:Name="RecurringPanelAdd" Visibility="Collapsed">
                            <TextBlock Text="Recurring Details" FontSize="16" FontWeight="Bold" Foreground="#1E3A8A" Margin="0,0,0,15"/>

                            <!-- Time of Day -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Departure Time" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <xctk:TimePicker x:Name="tpDepartureTimeRecurring" Padding="5" ValueChanged="DepartureTimeChanged"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Arrival Time (Auto)" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <xctk:TimePicker x:Name="tpArrivalTimeRecurring" Padding="5" IsEnabled="False" Background="#EEEEEE"/>
                                </StackPanel>
                            </Grid>

                            <!-- Date Range -->
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Start Date" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <DatePicker x:Name="dpStartDateAdd" Padding="5"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="End Date" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                                    <DatePicker x:Name="dpEndDateAdd" Padding="5"/>
                                </StackPanel>
                            </Grid>

                            <!-- Frequency -->
                            <TextBlock Text="Frequency" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,5"/>
                            <Border Background="#F7F9FC" CornerRadius="5" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,15">
                                <ComboBox x:Name="cmbFrequencyAdd" Padding="10" Background="Transparent" BorderThickness="0" SelectionChanged="FrequencyChanged">
                                    <ComboBoxItem Content="Daily"/>
                                    <ComboBoxItem Content="Weekly"/>
                                </ComboBox>
                            </Border>

                            <!-- Weekly Days Selection -->
                            <StackPanel x:Name="DaysSelectionPanel">
                                <TextBlock Text="Select Active Days" FontWeight="SemiBold" Foreground="#666666" Margin="0,0,0,10"/>
                                <WrapPanel Orientation="Horizontal">
                                    <CheckBox x:Name="chkMon" Content="Mon" Margin="0,0,15,10"/>
                                    <CheckBox x:Name="chkTue" Content="Tue" Margin="0,0,15,10"/>
                                    <CheckBox x:Name="chkWed" Content="Wed" Margin="0,0,15,10"/>
                                    <CheckBox x:Name="chkThu" Content="Thu" Margin="0,0,15,10"/>
                                    <CheckBox x:Name="chkFri" Content="Fri" Margin="0,0,15,10"/>
                                    <CheckBox x:Name="chkSat" Content="Sat" Margin="0,0,15,10"/>
                                    <CheckBox x:Name="chkSun" Content="Sun" Margin="0,0,15,10"/>
                                </WrapPanel>
                            </StackPanel>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>

                <!-- 3. ACTION BUTTON -->
                <Border Grid.Row="2" Padding="30,0,30,30">
                    <Button Content="Add Schedule"
                            Height="45"
                            Foreground="White"
                            FontWeight="Bold"
                            FontSize="14"
                            BorderThickness="0"
                            Click="AddScheduleAdd_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#1E3A8A"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#152869"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#2A4CB3"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Border>

            </Grid>
        </Border>
    </Grid>
</UserControl>