<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="TMS.Pages.Customer.CustomerBookingsPage"
      Title="CustomerBookingsPage"
      Background="#f0f4f8" av:DesignWidth="748.057">

    <Grid Margin="10">
        <StackPanel>

            <!-- Back Button -->
            <Button Click="Back_Click"
                    Height="40"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#17358A"
                    HorizontalContentAlignment="Left"
                    Padding="0"
                    Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="20" Height="20" Margin="0,0,10,0"/>
                    <TextBlock Text="Back" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <TextBlock Text="Your Booking History" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid x:Name="BookingsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                      Height="400">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Booking Ref" Binding="{Binding BookingReference}" Width="*"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding BookingDate, StringFormat=yyyy-MM-dd}" Width="*"/>
                    <DataGridTextColumn Header="Schedule ID" Binding="{Binding ScheduleId}" Width="*"/>
                    <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount}" Width="*"/>
                    <DataGridTextColumn Header="Discount" Binding="{Binding DiscountAmount}" Width="*"/>
                    <DataGridTextColumn Header="Final Amount" Binding="{Binding FinalAmount}" Width="*"/>
                    <DataGridTextColumn Header="Booking Status" Binding="{Binding BookingStatus}" Width="*"/>
                    <DataGridTextColumn Header="Payment Status" Binding="{Binding PaymentStatus}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

        </StackPanel>
    </Grid>
</Page>