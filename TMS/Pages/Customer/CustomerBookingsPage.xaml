<Page x:Class="TMS.Pages.Customer.CustomerBookingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      mc:Ignorable="av" 
      Title="CustomerBookingsPage"
      Background="#F0F2F5"
      FontFamily="Segoe UI">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <Button Click="Back_Click"
                    Height="40"
                    HorizontalAlignment="Left"
                    Background="Transparent"
                    BorderThickness="0"
                    Padding="0"
                    Foreground="#1E3A8A"
                    Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Back to Dashboard" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <TextBlock Text="My Booking History" FontSize="32" FontWeight="Bold" Foreground="#1E3A8A"/>
            <TextBlock Text="View details of your past and upcoming trips" FontSize="16" Foreground="Gray" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.05" Color="Black"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#1E3A8A" CornerRadius="12,12,0,0" Padding="20">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="TicketAccount" Width="24" Height="24" Foreground="White" Margin="0,0,10,0"/>
                        <TextBlock Text="All Transactions" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <DataGrid x:Name="BookingsDataGrid" 
                          Grid.Row="1"
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          IsReadOnly="True" 
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#EEEEEE"
                          Background="White"
                          BorderThickness="0"
                          Padding="10">

                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F7FA"/>
                            <Setter Property="Foreground" Value="#1E3A8A"/>
                            <Setter Property="Padding" Value="15,12"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        </Style>

                        <Style TargetType="DataGridRow">
                            <Setter Property="Padding" Value="0,8"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>

                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="15,10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                            <ContentPresenter VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ref Code" Binding="{Binding BookingReference}" Width="120" FontWeight="Bold"/>

                        <DataGridTextColumn Header="Date" Binding="{Binding BookingDate, StringFormat=dd MMM yyyy}" Width="120"/>

                        <DataGridTextColumn Header="Sch. ID" Binding="{Binding ScheduleId}" Width="80"/>

                        <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat=Rs. {0:N0}}" Width="*"/>
                        <DataGridTextColumn Header="Discount" Binding="{Binding DiscountAmount, StringFormat=Rs. {0:N0}}" Width="*"/>

                        <DataGridTextColumn Header="Final Paid" Binding="{Binding FinalAmount, StringFormat=Rs. {0:N0}}" Width="*" FontWeight="Bold" Foreground="#2E7D32"/>

                        <DataGridTextColumn Header="Booking Status" Binding="{Binding BookingStatus}" Width="140" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="Payment" Binding="{Binding PaymentStatus}" Width="100"/>
                    </DataGrid.Columns>

                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Page>