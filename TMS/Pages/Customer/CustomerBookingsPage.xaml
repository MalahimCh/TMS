<Page x:Class="TMS.Pages.Customer.CustomerBookingsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" Title="CustomerBookingsPage" Background="#F0F2F5" FontFamily="Segoe UI">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <!-- Back button and title remain same -->
        </StackPanel>
        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical">
                <!-- Pending Bookings -->
                <Border Background="White" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel>
                        <Border Background="#F57C00" CornerRadius="12,12,0,0" Padding="20">
                            <TextBlock Text="Pending Bookings" Foreground="White" FontSize="18" FontWeight="Bold"/>
                        </Border>
                        <DataGrid x:Name="PendingBookingsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE" Background="White" BorderThickness="0" Padding="10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ref Code" Binding="{Binding BookingReference}" Width="*"/>
                                <DataGridTextColumn Header="Schedule" Binding="{Binding ScheduleDisplay}" Width="*"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding BookingDate, StringFormat=dd MMM yyyy}" Width="*"/>
                                <DataGridTextColumn Header="Final Amount" Binding="{Binding FinalAmount, StringFormat=Rs. {0:N0}}" Width="*"/>
                                <!-- Action buttons -->
                                <DataGridTemplateColumn Header="Actions" Width="200">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" >
                                                <Button Content="Pay Now" Click="PayNow_Click" Background="#2E7D32" Foreground="White" Padding="8,4"/>
                                                <Button Content="Cancel" Click="CancelBooking_Click" Background="#C62828" Foreground="White" Padding="8,4"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
                <!-- Confirmed Bookings -->
                <Border Background="White" CornerRadius="12" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel>
                        <Border Background="#1E3A8A" CornerRadius="12,12,0,0" Padding="20">
                            <TextBlock Text="Confirmed Bookings" Foreground="White" FontSize="18" FontWeight="Bold"/>
                        </Border>
                        <DataGrid x:Name="ConfirmedBookingsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE" Background="White" BorderThickness="0" Padding="10">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ref Code" Binding="{Binding BookingReference}" Width="*"/>
                                <DataGridTextColumn Header="Schedule" Binding="{Binding ScheduleDisplay}" Width="*"/>
                                <DataGridTextColumn Header="Date" Binding="{Binding BookingDate, StringFormat=dd MMM yyyy}" Width="*"/>
                                <DataGridTextColumn Header="Final Amount" Binding="{Binding FinalAmount, StringFormat=Rs. {0:N0}}" Width="*"/>
                                <!-- Cancel only -->
                                <DataGridTemplateColumn Header="Actions" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Cancel" Click="CancelBooking_Click" Background="#C62828" Foreground="White" Padding="8,4"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>