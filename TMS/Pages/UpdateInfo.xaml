<Page x:Class="TMS.Pages.Admin.UpdateInfo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:TMS.Pages.Admin"
      xmlns:controls="clr-namespace:TMS.Controls"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Update Info"
      Background="#F5F7FF">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ============ SIDEBAR ============ -->
        <Border Background="#17358A" Grid.Column="0">
            <StackPanel Margin="0,20,0,0">

                <!-- BACK BUTTON (Same as Demo) -->
                <Button Click="Back_Click"
                        Height="50"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        HorizontalContentAlignment="Left"
                        Padding="20,0,0,0"
                        Margin="0,0,0,30">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ArrowLeft" Width="20" Height="20" Margin="0,0,10,0"/>
                        <TextBlock Text="Back" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- PAGE TITLE -->
                <TextBlock Text="Update Info"
                           FontSize="22"
                           Foreground="White"
                           FontWeight="Bold"
                           Margin="25,0,0,20"/>

                <!-- UPDATE INFO BUTTON -->
                <Button Tag="Info"
                        Click="Sidebar_Click"
                        Height="50"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalContentAlignment="Left"
                        Padding="25,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Information" Width="24" Height="24" Margin="0,0,15,0"/>
                        <TextBlock Text="Update Info" FontSize="15" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- CHANGE PASSWORD BUTTON -->
                <Button Tag="Password"
                        Click="Sidebar_Click"
                        Height="50"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalContentAlignment="Left"
                        Padding="25,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Lock" Width="24" Height="24" Margin="0,0,15,0"/>
                        <TextBlock Text="Change Password" FontSize="15" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </Border>

        <!-- ============ MAIN CONTENT AREA ============ -->
        <Grid Grid.Column="1" Margin="20">
            <ContentControl x:Name="ContentArea"/>
        </Grid>

    </Grid>
</Page>